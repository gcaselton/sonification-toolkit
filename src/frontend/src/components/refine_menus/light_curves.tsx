import {
  Box,
  Button,
  createListCollection,
  Checkbox,
  Field,
  Heading,
  Image,
  Input,
  Text,
  Flex,
  NumberInput,
  VStack,
  Select,
  HStack
} from "@chakra-ui/react";
import { RefineMenuProps } from "./RefineMenu";

export default function light_curves(dataFilepath: RefineMenuProps) {
    return(
        <Box>hello</Box>
    )
}